# PLC Work Package

`esp_supervisor.st` is a readable IEC 61131-3 logic baseline for state, permissive, degraded operation, controlled stop, trip and first-out behavior. It is not yet a vendor project file. Before runtime use, add timers, edge handling, retentive reset, analog-quality voting, task timing, target-specific function blocks and a verified I/O map.

